const { DataTypes } = require('sequelize');
const Model = require('../../vendor/db/Model');

const $nfu = new Model(
  '$name',
  {
    // Put your attributes here
  },
  {}
);

module.exports = $nfu;
